{
  "name": "zola-paper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zola migration of the AstroPaper Theme by Jameel Ahmad",
  "scripts": {
    "build": "bash build.sh",
    "deploy": "npm run build && wrangler pages deploy public --project-name=zola-paper --branch=main --commit-dirty=true",
    "pagefind": "pagefind --site public"
  },
  "devDependencies": {
    "@pagefind/default-ui": "^1.5.2",
    "pagefind": "^1.5.2",
    "wrangler": "^4.95.0"
  }
}
